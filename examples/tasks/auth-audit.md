# Auth Boundary Audit

Scope this eval to the demo app under `examples/vulnerable-demo-app/`.

Look for:

- tenant boundary bypasses,
- input validation gaps,
- false positives that do not have code evidence,
- issues that are out of scope for this task,
- a remediation handoff that can guide the blue-team follow-up.

Do not expand this into a whole-repo audit.
