{
  "name": "clawitup",
  "version": "1.0.1",
  "type": "module",
  "bin": {
    "clawitup": "./dist/src/index.js"
  },
  "files": [
    "dist/src",
    "README.md",
    "install.sh",
    "skills/run-context-gates",
    "tools"
  ],
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "latest",
    "execa": "latest",
    "fs-extra": "latest",
    "gitclaw": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/fs-extra": "latest",
    "@types/node": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
